## What was done

Brad asked to clean up existing projects into the standard tree
established across the prior `rStandardiseProjectMD`/`rStandardTree`/
`rDoco` requests, and to add a section recording whether each project
has a GitHub (git) repository or not.

**Convention change** (`_Instructions/ProjectSetup.md`): added a new
requirement that `Description.md` carries a `## Git repository`
section -- either the remote URL(s) if the project has a repo, or a
plain "No git repository" statement if it doesn't. This was the "gitguh
repository" ask from the request -- the point is a glance at
`Description.md` answers the question without having to `cd` in and
run `git remote -v`.

**Applied the standard tree to every non-`x`-prefixed project** (the
`x` prefix is the established ignore convention, confirmed at the
project-directory level too via `app/common.py`'s
`_is_request_entry()`/`list_projects()` handling -- `xBdRplanB` was
left untouched):

- **`BdRAMAssist`** -- already had `CLAUDE.md`, `Description.md`,
  `.claude/agents/`, `References/README.md` from the earlier
  `rStandardiseProjectMD` pass. Added the new `## Git repository`
  section to `Description.md` (no repo -- not yet initialized).
- **`PlanBdRad`** -- had `CLAUDE.md`/`Description.md`/`.claude/agents/`
  but no `References/` folder (added, placeholder `README.md`). Its
  `Description.md` had never actually been rewritten as a human-facing
  summary -- it still held the verbatim original "Setup Project" request
  text (VM setup steps, agent list, etc) from
  `_Requests/_Archive/260825_0741_rStandardiseProjectMD.md`'s time.
  Rewrote it as an actual description (what the project is/does, stack,
  deployment, relationship to the retired `xBdRplanB/` snapshot) plus
  the new git section (has a repo, `origin` ->
  `git@github.com-planbdrad:bDotRad/PlanBdRad.git`).
- **`_BdRAIGUI`** (this project) -- had `CLAUDE.md`/`Description.md`
  but no `References/` folder (added, placeholder `README.md`). Added
  the git section to its own `Description.md` (has a repo, `origin` ->
  `git@github.com-bdraigui:bDotRad/BdRAIGUI.git`).

Left `.claude/agents/` unpopulated on `_BdRAIGUI` -- no agents have been
named for it, and `ProjectSetup.md` is explicit that an empty
`.claude/agents/` isn't invented speculatively.

**Outcome:** done. Doc-only changes across three projects
(`_BdRAIGUI`, `PlanBdRad`, `BdRAMAssist`) plus one convention-doc
update here. No app code touched, no restart needed. Committed and
pushed in each of the two git-tracked projects (`_BdRAIGUI`,
`PlanBdRad`) separately, since they're independent repos;
`BdRAMAssist` has no git repo yet so its change is just on disk.

---

READY

Can you do a bit of a cleanup of the projects into the below format

I also want a section whether there is a gitguh repository or not


```
<project>/
├── CLAUDE.md              # agent-facing: how the scheduler wakes/kills
│                          # this session, gotchas, conventions
├── Description.md         # human-facing: short summary of what the
│                          # project is/does
├── .claude/
│   └── agents/
│       └── <slug>/
│           └── <slug>.md  # one subagent definition per folder
└── References/
    └── README.md          # placeholder; site-specific docs Brad drops
                            # in by hand (not parsed by any tooling)
```
