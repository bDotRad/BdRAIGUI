# r Logo Sq

**Request:** Replace the site logo with the new square version, and move it
from the top-right to the top-left of the page at 100x100.

**Found/Done:**
- Replaced `app/static/rat-logo.png` with the new square rat logo
  (`RatImageSq.png` from the request folder).
- Updated `#site-logo` CSS in `app/templates/index.html`: `right` -> `left`,
  size `84x84` -> `100x100`.
- The title (`h1`) and subtitle now start at the top-left corner where the
  logo now sits, so added `margin-left: 116px` to both so the logo doesn't
  overlap "BdRAIGUI Dashboard" and the subtitle text.
- Restarted the running dashboard process (it was already up on :8420, not
  under systemd) so the new template/CSS/image took effect immediately, and
  verified via curl that the served page reflects the changes.

**Outcome:** Fixed and deployed (live dashboard process restarted and
serving the updated logo/layout).
