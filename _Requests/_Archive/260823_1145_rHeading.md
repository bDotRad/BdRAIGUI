## What was done

Restructured the page header: it was previously a fixed-position logo
top-left with a tiny version string pinned underneath it, and a
separate `<h1>BdRAIGUI Dashboard</h1>` indented to clear the logo.
Changed to a `#header-row` flex row -- logo on the left, and to its
right a `#header-text` column with the heading ("BdR AI GUI", the
spaced form Brad asked for) on top and the version string directly
under it. Dropped the old fixed positioning and the manual left-margin
indents on `h1`/`.subtitle` that existed to work around it, since the
logo is now in normal document flow next to the heading instead of
floating over the page. (`app/templates/index.html`)

**Outcome:** fixed and deployed -- dashboard restarted (SIGKILL) and
verified live: `/` now serves the logo, "BdR AI GUI" heading, and
version string in the requested arrangement. Committed and pushed to
GitHub per the convention documented in the prior archive entry from
this same pass (260823_1140_rProjectOrder_rVersion_rInactiveProjects.md).

---

## Original request (verbatim)

READY

I want the main heading to be
BdR AI GUI

Under that text the version

Then the logo to the left.
